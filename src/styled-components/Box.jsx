import styled from 'styled-components';

export const Box = styled.article`
    width: 50%;
    display: flex;
    flex-wrap: wrap;
    padding: 10px;
    gap: 20px;

    h2{
        width: 100%;
    }
`;

